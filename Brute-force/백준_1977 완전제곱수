m=int(input())
n=int(input())
arr=[]

for i in range(1,101):
    i*=i
    if i>=m and i<=n:
        arr.append(i)
a=0
        
for i in arr:
    a+=i
    
if len(arr)>=1:
    print(a)
    print(arr[0])   
else: print('-1')
